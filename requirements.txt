openai>=1.40.0
python-dotenv>=1.0.0
piper-tts>=1.2.0
gtts>=2.5.0
faster-whisper>=1.0.0
sounddevice>=0.4.6
numpy>=1.24.0
rich>=13.7.0
# VAD neural Silero — WAJIB untuk penangkapan suara (tak ada fallback RMS lagi).
# Modelnya (voca/silero_vad.onnx) dijalankan onnxruntime — runtime inferensi
# ringan yang juga dipakai Piper. Tak perlu torch (~1GB) lagi: model identik,
# hasil deteksi sama, install jauh lebih kecil.
onnxruntime>=1.16
